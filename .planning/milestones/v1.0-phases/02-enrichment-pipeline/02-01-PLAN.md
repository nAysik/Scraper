---
phase: 02-enrichment-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/pipeline/categorize.ts
autonomous: true
requirements: []
tags: [cleanup, dead-code, deletion, perplexity, openai]

must_haves:
  decisions_addressed: [D-09]
  truths:
    - "src/lib/pipeline/categorize.ts no longer exists in the repo"
    - "No file under src/ imports from src/lib/pipeline/categorize"
    - "tsc --noEmit passes after deletion"
    - "npm run lint passes after deletion"
  artifacts:
    - path: "src/lib/pipeline/categorize.ts"
      provides: "MUST NOT EXIST after this plan completes (deletion target per D-09)"
  key_links:
    - from: "src/app/api/scrape/route.ts"
      to: "src/lib/pipeline/keyword-categorize.ts"
      via: "import (already in place — must NOT change)"
      pattern: "from '@/lib/pipeline/keyword-categorize'"
---

<objective>
Delete `src/lib/pipeline/categorize.ts` per CONTEXT.md D-09. The file implements a Perplexity-based AI categorizer that is never called by any route handler — verified dead code (research §State of the Art line 800: "Verified dead code: grep for imports across src/ returns zero hits except for the file's own internal calls"). All active categorization uses `src/lib/pipeline/keyword-categorize.ts` (`categorizeByKeywords`).

Purpose: Remove a documented architectural anti-pattern (`.planning/codebase/ARCHITECTURE.md` §Anti-Patterns line 217-223) and unblock the env-table swap of `PERPLEXITY_API_KEY` → `OPENAI_API_KEY` (Plan 05). This plan is independent and runs in Wave 1 in parallel with Plan 02.

Output: One file deleted, type-check and lint still pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-enrichment-pipeline/02-CONTEXT.md
@.planning/phases/02-enrichment-pipeline/02-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify zero imports of categorize.ts and delete the file</name>
  <files>src/lib/pipeline/categorize.ts</files>
  <read_first>
    - src/lib/pipeline/categorize.ts (the file being deleted — read first to confirm exports being removed)
    - .planning/phases/02-enrichment-pipeline/02-CONTEXT.md (D-09 — locked decision to delete)
    - .planning/phases/02-enrichment-pipeline/02-RESEARCH.md §State of the Art line 800 (verifies dead code)
    - src/app/api/scrape/route.ts (confirms the only category import is `keyword-categorize`, NOT `categorize`)
    - src/app/api/cron/scrape/route.ts (same — uses `keyword-categorize`)
    - src/app/api/cron/shorts/route.ts (same — uses `keyword-categorize`)
  </read_first>
  <action>
Per D-09 (locked): delete `src/lib/pipeline/categorize.ts`.

Step-by-step:

1. Run a final import check to confirm zero non-self imports across `src/`:
   ```powershell
   $env:PATH = "C:\Program Files\nodejs;" + $env:PATH
   # Use grep tool / ripgrep equivalent — search for any import of the categorize module path
   # Expected matches across src/: ZERO (the file itself is not in src/ once deleted; only its own self-references should appear pre-deletion)
   ```
   Specifically search the patterns below. If any match shows up in a file OTHER than `src/lib/pipeline/categorize.ts` itself, STOP and report — do not proceed with deletion.
   - `from ['"]@/lib/pipeline/categorize['"]`
   - `from ['"]\.\./pipeline/categorize['"]`
   - `from ['"]\./categorize['"]` (only meaningful if matched outside the file)
   - `require\(['"].*pipeline/categorize['"]\)`
   - `categorizeVideos\(` and `categorizeInBatches\(` (the two exported function names)

2. Delete the file:
   ```powershell
   $env:PATH = "C:\Program Files\nodejs;" + $env:PATH
   Remove-Item "src/lib/pipeline/categorize.ts"
   ```
   (Or `rm src/lib/pipeline/categorize.ts` via bash.)

3. Verify the project still compiles and lints:
   ```powershell
   $env:PATH = "C:\Program Files\nodejs;" + $env:PATH
   npx tsc --noEmit
   npm run lint
   ```
   Both MUST exit 0. The expected output of each is silent success (tsc) and "✔ No ESLint warnings or errors" or equivalent (lint).

DO NOT touch `src/lib/pipeline/keyword-categorize.ts` — that is the ACTIVE categorizer, used by all three route handlers (`/api/scrape`, `/api/cron/scrape`, `/api/cron/shorts`). Confirmed via grep in research.

DO NOT touch `.env.local` or `CLAUDE.md` in this plan. The env-table swap (`PERPLEXITY_API_KEY` → `OPENAI_API_KEY`) is Plan 05's responsibility (it depends on Plan 02 completing the OpenAI integration first).

DO NOT remove the `openai` package from `package.json` — Plan 02 reuses it for the gpt-4o-mini extractor (`src/lib/outreach/extract-games.ts`). The package stays; only the Perplexity-flavoured wrapper goes.
  </action>
  <verify>
    <automated>
      Run from repo root with Node on PATH:
      ```
      $env:PATH = "C:\Program Files\nodejs;" + $env:PATH
      if (Test-Path "src/lib/pipeline/categorize.ts") { exit 1 }
      npx tsc --noEmit
      if ($LASTEXITCODE -ne 0) { exit 1 }
      npm run lint
      if ($LASTEXITCODE -ne 0) { exit 1 }
      exit 0
      ```
    </automated>
  </verify>
  <acceptance_criteria>
    - `Test-Path src/lib/pipeline/categorize.ts` returns `False` (file deleted)
    - `npx tsc --noEmit` exits 0 (type-check passes)
    - `npm run lint` exits 0 (lint passes)
    - Grep for `from '@/lib/pipeline/categorize'` across `src/` returns ZERO matches
    - Grep for `categorizeVideos\(|categorizeInBatches\(` across `src/` returns ZERO matches
    - File `src/lib/pipeline/keyword-categorize.ts` is UNCHANGED (`git diff src/lib/pipeline/keyword-categorize.ts` shows no modifications)
    - `package.json` is UNCHANGED in this plan (no dependency removal)
  </acceptance_criteria>
  <done>
    `src/lib/pipeline/categorize.ts` is deleted; tsc and lint both pass; no other source files were modified; the `openai` npm package remains installed for Plan 02 to reuse.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo file system | Deletion of a source file — local-only operation, no runtime surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `src/lib/pipeline/categorize.ts` deletion | accept | Pure cleanup of unused server-side code; no runtime behaviour changes because no caller imports it (verified by grep in research §State of the Art). Type-check + lint gate catch any accidental import we missed. |
| T-02-02 | Denial of Service | Unintended cascade if a hidden caller exists | mitigate | Pre-deletion grep covers all 5 import patterns enumerated above. Post-deletion `npx tsc --noEmit` is the type-system safety net — TypeScript will fail the build if any unresolved `@/lib/pipeline/categorize` import survives. |
| T-02-03 | Information Disclosure | Stale `PERPLEXITY_API_KEY` in `.env.local` | accept | Out of scope for this plan; Plan 05 documents the env-table swap. No active code reads the var after categorize.ts is deleted, so leaving the line in `.env.local` is inert. |
</threat_model>

<verification>
After this plan runs, the codebase contains no Perplexity-flavoured AI categorizer. The active keyword categorizer (`src/lib/pipeline/keyword-categorize.ts`) is the sole categorization path. Type-check and lint serve as the safety net against accidental import survival. No runtime behaviour changes.
</verification>

<success_criteria>
- [ ] `src/lib/pipeline/categorize.ts` no longer exists
- [ ] `npx tsc --noEmit` passes
- [ ] `npm run lint` passes
- [ ] No other files in `src/` are modified
- [ ] `package.json` and `package-lock.json` are unchanged (the `openai` package stays for Plan 02 to reuse)
</success_criteria>

<output>
After completion, create `.planning/phases/02-enrichment-pipeline/02-01-SUMMARY.md` documenting:
- File deleted (with sha of the pre-delete content for reference)
- tsc + lint exit codes
- Confirmation that `keyword-categorize.ts` was untouched
- Note for Plan 05: env-table swap is now safe to perform
</output>
